typeset -i numvols=16
typeset -i endvol=$numvols-1

hostname=$(hostname -f)
volsize=13200

for i in $(seq 0 $endvol)
do
   volname="cbt-librbdfio-${hostname}-${i}"
   cephadm shell rbd create --pool rbd_replicated --data-pool rbd_erasure --size $volsize $volname
done

